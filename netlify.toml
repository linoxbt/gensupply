[build]
  base = "frontend"
  command = "npm run build"
  # Resolved from the repo root, not the base dir.
  publish = "frontend/.next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "22"
